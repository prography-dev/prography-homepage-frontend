import React from 'react';
import styles from './StyledTest.module.scss';

const StyledTest = () => {
  return (
    <button className={styles.button} type="button">
      StyledTest
    </button>
  );
};

export default StyledTest;
